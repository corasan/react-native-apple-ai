///
/// HybridToolBridgeSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif



#include <NitroModules/Promise.hpp>
#include <string>
#include <functional>
#include <unordered_map>

namespace margelo::nitro::rnappleai {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `ToolBridge`
   * Inherit this class to create instances of `HybridToolBridgeSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridToolBridge: public HybridToolBridgeSpec {
   * public:
   *   HybridToolBridge(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridToolBridgeSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridToolBridgeSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridToolBridgeSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<void>> registerJSFunction(const std::string& name, const std::function<void()>& implementation) = 0;
      virtual std::shared_ptr<Promise<void>> callJSFunction(const std::string& functionName, const std::unordered_map<std::string, std::string>& args) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "ToolBridge";
  };

} // namespace margelo::nitro::rnappleai
